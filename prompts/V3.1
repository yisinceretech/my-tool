# Little Body Detective_Prompt

- 版本: v3.0.0 
- 設計目標: 結合「第一堂人體課」課綱，擴充消化道（胃、小腸、大腸）與過濾器官（脾、腎），並加入「營養去哪了」動態展示與「對照實驗」教學模式。確保單檔運行無外部依賴。

🤖 系統提示詞 (System Prompt)

你是一個精通 Web Audio API、Three.js (r128) 以及互動式教學 UI 開發的資深前端工程師。請幫我開發一個「Little Body Detective_Prompt」的網頁應用程式。

## 【核心嚴格要求】
1. **單檔完美運行**：請做成「單一個 HTML 檔」，將 HTML、CSS、JavaScript 全部寫在一起。
2. **無干擾體驗**：絕對不要使用 `alert`、`confirm`、`prompt`。
3. **完整不分段**：必須提供完整可以直接複製的程式碼，不要分段或省略，方便直接貼上 GitHub 部署。
4. **零外部依賴**：除了載入 Three.js CDN (必須使用穩定的 r128 版本) 之外，所有 3D 模型與聲音皆由純程式碼與幾何圖形生成。

## 【版面與 UI 教學面板設計】
* **佈局**：左側為 UI 控制與教學面板（半透明深色毛玻璃質感），右側為 3D 畫布。
* **2D 監視器畫布 (Canvas)**：繪製滾動的時間軸波形。必須能根據選中的器官呈現不同視覺（如心電圖、腸胃波塊、液體高頻波紋等）。
* **教學控制項目（新增）**：
    * **主控台**：「▶️ 開始監聽 / ⏹ 停止監聽」（停止時需完全靜默）。
    * **聽診器官選擇器**：按鈕包含 心臟🫀、胃部🟢、小腸🟡、大腸🟤、脾臟🟣、腎臟🔵。
    * **【教案一：對照實驗】**：提供 A/B 狀態快速切換按鈕。例如「一般狀態」對比「咖啡因」、「高纖維」對比「精緻糖」，切換時聲音、3D 表情與波形需瞬間改變，方便學生對比。
    * **【教案二：營養去哪了】**：一顆專屬的「🔄 消化路徑追蹤」按鈕。點擊後，系統會依序自動切換聽診焦點：胃(停留3秒) -> 小腸(停留3秒) -> 大腸(停留3秒)，並且 3D 模型上的器官會依序亮起，波形與聲音也平滑過渡。

## 【3D 渲染與防彈級穩定度 (Three.js)】
* **極致可愛 Q 版人物設計 (嚴格 2 頭身)**：必須表現出幼態、軟萌的可愛感。全部以 `SphereGeometry` 與 `CylinderGeometry` 憑空生成。
    * **頭身比例 (1:1.2)**：頭部 (`SphereGeometry`) 半徑必須極大，身體 (`CylinderGeometry`) 必須極短。頭部與身體的高度比例幾乎是一樣的。
    * **臉部五官 (低重心法則)**：
        * **眼睛**：使用黑色大圓球 (`SphereGeometry`)，尺寸要大，並且位置必須「低於臉部水平中線」，眼距稍微拉開，營造無辜感。
        * **嘴巴**：極小的紅色幾何體，位置靠近下巴。
        * **腮紅 (靈魂重點)**：必須在眼睛斜下方加入兩片粉紅色、半透明的壓扁圓柱體 (`CylinderGeometry`) 作為腮紅。
    * **四肢設計 (短胖笨拙)**：手臂與腿部請使用較粗短的圓柱體，不要有任何肌肉或關節細節，手腳長度不可超過身體軀幹的長度，呈現如布偶般的圓潤感。
    * **材質與服飾**：穿著半透明衣服 (`MeshStandardMaterial`, `transparent: true, opacity: 0.5`)。
     * **【外觀與服飾】
人物留著（蓬鬆的棕色短髮），穿著（一件寬鬆的淺藍色連帽衫和米色短褲）。（連帽衫上有一個可愛的小熊圖案）。圓圓的小手（small, round balled hands）和（穿著運動鞋的小腳）。

    * **【風格與細節】
整體風格簡約大方（simplified design），線條圓潤流暢（smooth lines）。（服飾材質看起來柔軟舒適）。背景乾淨簡約，（採用柔和的粉彩色調，略帶透明感）。
* **核心防呆修正**：絕對【不要】使用 `ViewOffset` 與 `transmission` 材質。將 3D 模型實體的 X 軸向右推移 (`model.position.x = 1.5`) 以完美置中。全部使用基礎幾何體，絕對【不要使用】`CapsuleGeometry`，確保 r128 不報錯。執行這段,我要看人物跑出來長什麼樣子
* **內部器官擴充 (重點)**：
    * **心臟 (Heart)**：胸口，發紅光。
    * **胃部 (Stomach)**：上腹部，發綠光。
    * **小腸 (Small Intestine)**：中腹部，使用多個交錯的彎曲圓柱或環狀體 (`TorusGeometry`) 組合，發出黃橘光。
    * **大腸 (Large Intestine)**：圍繞在小腸外圍的粗管體（可使用粗的 `TorusGeometry` 裁切或拼接），發出深褐/深綠光。
    * **脾臟 (Spleen)**：胃的左上方，較小的深紫色扁橢圓形。
    * **腎臟 (Kidneys)**：後腰側左右各一顆，藍色的豆狀球體。
* **互動**：加入 Raycaster，滑鼠直接點擊上述任何一個器官，即可切換聽診部位，未選中的器官自動變暗。

## 【Web Audio API 聲音引擎 (器官模擬擴充)】
所有音訊必須設為 `loop = true`。請為不同器官設計專屬的 DSP 邏輯：
* **心臟**：低頻正弦波 (45Hz, 65Hz) 搭配指數衰減包絡線 (Lub-dub)。
* **胃部**：白噪音通過低通濾波 (約 200Hz) 與 3Hz LFO 調變 (咕嚕聲)。
* **小腸 (新增)**：較高頻的水流與氣泡聲。使用白噪音通過帶通濾波器 (Bandpass Filter, 約 800-1200Hz) 搭配較快且不規則的 LFO 模擬液體快速推進。
* **大腸 (新增)**：低沉的氣體與殘渣摩擦聲。使用低通濾波器 (約 100Hz) 搭配極慢的 LFO (0.5Hz) 與輕微的失真 (Distortion/Overdrive) 模擬厚實的蠕動。
* **脾/腎 (新增)**：微弱而穩定的血液過濾背景音。使用粉紅噪音 (Pink Noise) 設定極低的音量與平穩無起伏的包絡線，營造靜態工作的嗡嗡聲 (Hum)。

## 【生理反射與連動邏輯】
* **視覺與聽覺解耦**：聽診特定器官時，只有該器官會有縮放跳動動畫，其他器官進入靜止待機狀態。
* **狀態連動**：
    * **☕ 咖啡因**：心跳極快，女孩表情驚恐(O型嘴/挑眉)，全身微發抖。
    * **🥦 高纖維**：小腸與大腸發出明亮健康的光芒，聲音飽滿厚實，女孩呈現彎月笑眼。
    * **🥤 精緻糖**：胃部發出警告紅光，小腸/大腸發出斷續空洞的噪音，女孩皺眉表情痛苦抽搐。
